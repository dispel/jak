# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|

  # config.vm.box = "ubuntu/trusty64"
  # config.vm.box = "jak.2016.11.16.box"
  config.vm.box = "https://s3.amazonaws.com/jak.releases/jak20161116v2.box"

  # config.ssh.private_key_path = '/Users/chris/.vagrant.d/boxes/jak.2016.11.16.box/0/virtualbox/vagrant_private_key'

  # config.vm.provider "virtualbox" do |vb|
  #   # vb.memory = "2048"
  #   # vb.cpus = 2
  # end
end

# How the actual box is setup.
# sudo su
# apt-get update
# add-apt-repository ppa:fkrull/deadsnakes
# apt-get update
# apt-get install python2.7 python3.5 pypy
# apt-get install python-pip
# apt-get install build-essential libssl-dev libffi-dev python-dev python3-dev git # for building cryptography among other things.
# Wasn't so hard was it?
